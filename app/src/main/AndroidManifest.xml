<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.dungeons_and_dragons">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar"
        tools:ignore="ExtraText">
        <activity android:name=".ui.AddEditCampaignActivity"
            android:theme="@style/Theme.Design"></activity>
        <activity android:name=".ui.AddEditCharacterActivity"
            android:theme="@style/Theme.Design"/>
        <activity android:name=".ui.AddEditMonsterActivity"
            android:theme="@style/Theme.Design"/>
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity" />
        <activity
            android:name=".NavActivity"
            android:theme="@style/Theme.Design" />
    </application>

</manifest>